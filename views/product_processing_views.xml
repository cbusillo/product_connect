<odoo>
    <!-- Multi-measure Graph View for Product Processing -->
    <record id="view_product_processing_graph" model="ir.ui.view">
        <field name="name">product.processing.graph</field>
        <field name="model">product.template</field>
        <field name="type">graph</field>
        <field name="arch" type="xml">
            <graph string="Product Processing Report" type="line">
                <field name="is_ready_for_sale_last_enabled_date" interval="month"/>
                <field name="initial_price_total" type="measure" string="Revenue Value"/>
                <field name="initial_cost_total" type="measure" string="Cost Value"/>
                <field name="initial_quantity" type="measure" string="Units Processed"/>
            </graph>
        </field>
    </record>

    <!-- Pivot View for Detailed Analysis -->
    <record id="view_product_processing_pivot" model="ir.ui.view">
        <field name="name">product.processing.pivot</field>
        <field name="model">product.template</field>
        <field name="arch" type="xml">
            <pivot string="Product Processing Report" sample="1">
                <field name="is_ready_for_sale_last_enabled_date" type="row" interval="month"/>
                <field name="source" type="col"/>
                <field name="initial_quantity" type="measure" string="Total Units"/>
                <field name="initial_price_total" type="measure" string="Revenue Value"/>
                <field name="initial_cost_total" type="measure" string="Cost Value"/>
            </pivot>
        </field>
    </record>

    <!-- Search View with Predefined Filters -->
    <record id="view_product_processing_search" model="ir.ui.view">
        <field name="name">product.processing.search</field>
        <field name="model">product.template</field>
        <field name="arch" type="xml">
            <search string="Product Processing">
                <field name="name"/>
                <field name="default_code"/>
                <field name="source"/>
                <field name="is_ready_for_sale_last_enabled_date"/>

                <filter string="Ready for Sale" name="ready_for_sale" domain="[('is_ready_for_sale', '=', True)]"/>
                <filter string="Processed Today" name="processed_today"
                        domain="[('is_ready_for_sale_last_enabled_date', '&gt;=', context_today().strftime('%Y-%m-%d'))]"/>
                <filter string="Processed This Week" name="processed_week"
                        domain="[('is_ready_for_sale_last_enabled_date', '&gt;=', (context_today() - relativedelta(days=7)).strftime('%Y-%m-%d'))]"/>
                <filter string="Processed This Month" name="processed_month"
                        domain="[('is_ready_for_sale_last_enabled_date', '&gt;=', context_today().strftime('%Y-%m-01'))]"/>

                <separator/>

                <filter string="Motor Products" name="motor_products" domain="[('source', '=', 'motor')]"/>
                <filter string="Import Products" name="import_products" domain="[('source', '=', 'import')]"/>
                <filter string="Shopify Products" name="shopify_products" domain="[('source', '=', 'shopify')]"/>

                <separator/>

                <filter string="Processed Date" name="groupby_processed_date"
                        context="{'group_by': 'is_ready_for_sale_last_enabled_date:month'}"/>
                <filter string="Source" name="groupby_source" context="{'group_by': 'source'}"/>
                <filter string="Part Type" name="groupby_part_type" context="{'group_by': 'part_type'}"/>
            </search>
        </field>
    </record>

    <!-- Action for Product Processing Analytics -->
    <record id="action_product_processing_analytics" model="ir.actions.act_window">
        <field name="name">Product Processing Report</field>
        <field name="res_model">product.template</field>
        <field name="view_mode">graph,pivot,list</field>
        <field name="domain">[('is_ready_for_sale', '=', True),
                              ('is_ready_for_sale_last_enabled_date', '!=', False)]</field>
        <field name="context">{
            'search_default_processed_month': 1,
            'search_default_ready_for_sale': 1,
            'graph_measures': ['initial_price_total', 'initial_cost_total', 'initial_quantity'],
            'pivot_measures': ['initial_price_total', 'initial_cost_total'],
        }</field>
        <field name="search_view_id" ref="view_product_processing_search"/>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                No products have been processed yet
            </p>
            <p>
                This report shows the value of products that have been processed and marked as ready for sale.
                You can analyze by time period, source, and product type to track your processing efficiency.
            </p>
        </field>
    </record>

    <!-- Set specific views for the action -->
    <record id="action_product_processing_analytics_graph" model="ir.actions.act_window.view">
        <field name="sequence" eval="1"/>
        <field name="view_mode">graph</field>
        <field name="view_id" ref="view_product_processing_graph"/>
        <field name="act_window_id" ref="action_product_processing_analytics"/>
    </record>

    <record id="action_product_processing_analytics_pivot" model="ir.actions.act_window.view">
        <field name="sequence" eval="2"/>
        <field name="view_mode">pivot</field>
        <field name="view_id" ref="view_product_processing_pivot"/>
        <field name="act_window_id" ref="action_product_processing_analytics"/>
    </record>

    <!-- Menu Items -->
    <menuitem id="menu_product_processing_report"
              name="Product Processing"
              parent="stock.menu_warehouse_report"
              action="action_product_processing_analytics"
              sequence="20"/>
</odoo>